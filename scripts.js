//math functions

const add = function(a) {
    return a + a;
}

const subtract = function(a) {
    return a - a;
}

const multiply = function(a) {
    return a * a;
}

const divide = function(a) {
    return a / a;
}